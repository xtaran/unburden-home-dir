# -*- shell-script -*-
# /etc/X11/Xsession.d/95dphys_unburden_home_dir
# This file is sourced by Xsession(5), not executed.

if [ -e /etc/default/dphys_unburden_home_dir ]; then
        . /etc/default/dphys_unburden_home_dir
fi

if [ "${DPHYS_UNBURDEN_HOME_DIR}" != "false" -a \
     "${DPHYS_UNBURDEN_HOME_DIR}" != "no" ]; then

    # Clean up this and that

fi
